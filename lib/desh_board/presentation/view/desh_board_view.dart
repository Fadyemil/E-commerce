import 'package:flutter/material.dart';

class DeshBoardView extends StatelessWidget {
  const DeshBoardView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DeshBoardView'),
      ),
    );
  }
}
